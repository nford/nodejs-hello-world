version: 0.2         

phases:
  install:
      runtime-versions:
          nodejs: 10
  build:
    commands:
      - npm install
      - npm run test
      
artifacts:
  files:
    - index.js
    - package.json
    - node_modules


